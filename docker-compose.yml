version: '3.8'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: paradiced-web:latest
    ports:
      - "0.0.0.0:5173:80"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
